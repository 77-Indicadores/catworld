import type { NextRequest } from "next/server"; import { prisma } from "@/server/db"; import { resolveActor } from "@/server/auth/actor"; import { canAccess } from "@/server/auth/permissions"; import { ensureInternalPrincipal,executeReadOnly,grantSchema } from "@/server/azure/sql"; import { executePostgresReadOnly } from "@/server/connections/postgres"; import { ApiError,handleApiError,ok } from "@/server/http"; import { quoteIdentifier } from "@/server/security/naming";
export async function GET(request:NextRequest,{params}:{params:Promise<{id:string}>}){try{const actor=await resolveActor(request),table=await prisma.datasetTable.findUniqueOrThrow({where:{id:(await params).id},include:{dataset:true,source:{include:{connection:true}}}});if(!(await canAccess(actor,"READ",table.dataset.projectId,table.dataset.id))&&actor.role!=="ADMIN")throw new ApiError(403,"FORBIDDEN","Sem acesso ao dataset");const limit=Math.min(Number(request.nextUrl.searchParams.get("limit")??100),1000);if(table.source?.mode==="live"){const source=table.source,sql=source.sourceKind==="table"?`SELECT * FROM "${source.sourceSchema}"."${source.sourceTable}"`:source.sourceSql!;const result=await executePostgresReadOnly(source.connection,sql,30,limit);return ok(result.rows,{columns:result.columns,rowCount:result.rowCount,source:{id:source.id,mode:source.mode}})}await ensureInternalPrincipal(actor.principal);await grantSchema(actor.principal,table.dataset.schemaName,"READ");const result=await executeReadOnly(actor.principal,`SELECT TOP ${limit} * FROM ${quoteIdentifier(table.dataset.schemaName)}.${quoteIdentifier(table.sqlName)}`,30,limit);return ok(result.rows,{columns:result.columns,rowCount:result.rowCount});}catch(e){return handleApiError(e)}}
